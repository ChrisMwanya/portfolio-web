export type ProjectType = {
  title: string;
  description: string;
  link?: string;
  technos?: string[];
  image?: string; // Optional: URL to project screenshot/preview
  imageAlt?: string; // Optional: Alt text for the image
};
